+++
id = "T-001"
title = '"@" mentions appear to be broken, fix this'
status = "done"
priority = 3
created_at = "2026-05-29T03:38:09Z"
updated_at = "2026-05-29T03:47:49Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "e35cbad34750005a"
+++
## Notes

- 2026-05-29T03:40:21Z [sarocu] PLAN — fix @ mentions
Files: src/components/chat/ChatInput.tsx (mention logic), src/components/markdown/MarkdownRenderer.tsx (response render), src/theme/global.css:1315-1357 (.mention-menu).

Diagnose first (root symptom unconfirmed):
- Confirm getMentionOptions() (ChatInput.tsx:176-188) actually returns rows: it reads days.value and skips todo.completed. If days signal isn't loaded in chat context, menu is empty -> 'broken'. Log options.length.
- Confirm @ trigger regex /@([^\s@]*)$/ (line 266) fires (cursor at end-of-token only; will not retrigger if user edits mid-word).

Likely fixes:
- ChatInput.tsx:261 filter prev.filter(m => value.includes(m.display)) drops a mention as soon as the user backspaces/edits the inserted '@Title' text; mentions also lost if two titles share a substring. Track mentions by id+span rather than substring match, or skip filtering and reconcile in expandMentions via regex over '@Title' tokens.
- expandMentions() (126-135) only expands titles present in mentions[] state; if state is out of sync nothing expands. Add fallback: match '@<title>' against current days todos to recover id/date.
- Verify cursor math insertMention():212 (before.length+display.length+1) lands after trailing space.
- MarkdownRenderer.tsx: agent echoes @[Title](id:..|date:..); marked renders as a link with junk href. Add a pre-parse pass to turn @[Title](id|date) into a styled <span class=mention> Title.

Verify: pnpm/npm build (tsc) clean; run app, open chat, type '@', confirm dropdown lists incomplete todos, select one, send, confirm payload contains @[Title](id:..|date:..) (log onSend), confirm response renders mention styled not as broken link. Edit/backspace the inserted text and confirm no crash and correct expansion.
- 2026-05-29T03:47:49Z [sarocu] DONE — root cause was render-side, not input-side. Dropdown/insertion work (days load on App mount). The mention token @[Title](id:..|date:..) (produced by expandMentions + buildSummaryPrompt) was rendered raw: plain markup in user bubbles (MessageBubble <span>) and a broken markdown link in assistant bubbles (marked turned [Title](id:..|date:..) into an <a> with junk href).

Fix:
- NEW src/components/markdown/mentions.tsx: MENTION_RE, mentionsToHtml() (pre-markdown -> styled <span class=mention> with HTML-escaped title + data-todo-id/date), and <MentionText> component for plain bubbles.
- MarkdownRenderer.tsx: run mentionsToHtml() before marked.parse so assistant echoes render as chips, not links. DOMPurify keeps span/class/data-*.
- MessageBubble.tsx: user bubble now renders <MentionText> instead of raw {content}.
- global.css: added .mention chip style (accent color + tinted bg via color-mix, fallback #4f8cff).

Verify:
- tsc --noEmit clean (npm run build's tsc step).
- Deterministic node check of the transform vs the exact token format: handles single/multiple mentions, escapes <>&, passes through non-mention text.
CLICK-TEST NOT POSSIBLE in this sandbox: WAYLAND_DISPLAY=wayland-1 is set but /run/user/1000 has no wayland socket (root-owned), user not in 'input' group, and the WebKit window fails on EGL/amdgpu. 'computer windows' returns 'Could not find wayland compositor'. App was built+launched via tauri dev (vite on :1420, beforeDevCommand bun stubbed out) but cannot be screenshotted/driven here. Visual confirmation of the chip styling should be done on a real desktop session.
