+++
id = "T-002"
title = "Add an edit feature for text based artifacts with awareness of file type (e.g. markdown vs code vs text)"
status = "done"
priority = 3
created_at = "2026-05-29T03:38:09Z"
updated_at = "2026-05-29T03:50:41Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "f4292fb5900cfe56"
+++
## Notes

- 2026-05-29T03:40:32Z [sarocu] PLAN — edit feature for text artifacts (file-type aware)
Backend already done: save_artifact(path, content)->Artifact exists at src-tauri/src/commands/artifacts.rs:119-144 and is registered in src-tauri/src/lib.rs. Path safety via resolve_relative().

Steps:
1. src/api/commands.ts: add saveArtifact(path, content) wrapper (invoke 'save_artifact'); mirror existing readArtifact at ~126. Add to exports.
2. src/components/artifacts/ArtifactsPanel.tsx (viewer modal ~221-243): add isEditing state + draft string. Toggle 'Edit' button in modal header. When editing, render a <textarea class=artifact-editor> bound to draft (default to loaded content). Show Save / Cancel. Save calls saveArtifact(viewing.relative_path, draft), then refresh content + re-list artifacts (size/created_at change). Cancel reverts draft.
3. File-type awareness: replace single isMarkdown check (ArtifactsPanel.tsx:135) with a helper kindForName(name): 'markdown'|'code'|'text' using extension (reuse the ext map already in ArtifactItem.tsx:20-38). View mode: markdown->MarkdownRenderer, code->syntax-highlighted <pre>, text->plain <pre>. Edit mode: plain textarea for all; for code, set spellcheck=false + monospace + tab handling (insert 2 spaces on Tab). Avoid pulling a heavy editor (Monaco/CodeMirror) unless desired — start with textarea.
4. Optional highlight for code view: only add a lib (e.g. highlight.js) if we want read-mode highlighting; not required for edit. Note as follow-up.
5. CSS: add .artifact-editor (textarea: 100% w/h of modal body, monospace, no resize jitter) + .artifact-edit-actions in src/theme/global.css near existing .artifact-plain.
6. Guard: only show Edit for !is_dir and text-ish kinds (skip png/jpg/svg/binary).

Verify: tsc/build clean; run app, open a .md and a .ts artifact, Edit -> change -> Save -> reopen, content persisted; confirm Cancel discards; confirm binary files show no Edit button; confirm list reflects new size.
- 2026-05-29T03:50:41Z [sarocu] DONE — file-type-aware artifact editing.
- src/api/commands.ts: added saveArtifact(path, content) -> Artifact wrapper for the existing save_artifact command.
- src/components/artifacts/ArtifactsPanel.tsx: added kindForName() (markdown|code|text|binary via ext sets; unknown/no-ext defaults to text=editable, binary skips edit). Modal header now has Edit (when canEdit = !is_dir && kind!=binary && !loadError) / Save / Cancel. Editing renders a <textarea class=artifact-editor> (code variant: spellcheck off, white-space:pre, Tab inserts 2 spaces). View mode: markdown->MarkdownRenderer, code->pre.artifact-code, text->pre.artifact-plain. Save calls saveArtifact, updates content, refreshes list (size/created_at). Escape cancels edit before closing modal; switching files resets edit state.
- src/theme/global.css: added .artifact-modal-actions, .artifact-editor(+:focus,-code), .artifact-code.

Verify: tsc --noEmit clean; vite build clean (119 modules).
CLICK-TEST NOT POSSIBLE: WAYLAND_DISPLAY=wayland-1 set but no wayland socket in /run/user/1000 and user not in 'input' group (same env limitation as T-001). Visual/interaction confirmation needs a real desktop session.
