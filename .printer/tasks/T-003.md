+++
id = "T-003"
title = 'change the name of the app to simply "planner"'
status = "done"
priority = 3
created_at = "2026-05-29T03:38:09Z"
updated_at = "2026-05-29T03:52:26Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "4627049df8069b2e"
+++
## Notes

- 2026-05-29T03:40:43Z [sarocu] PLAN — rename app to 'planner'
Current name is 'txture'. Scope = user-visible display name only; do NOT churn the Rust crate name or bundle identifier unless explicitly wanted (risky: identifier change moves app data dir; crate rename touches main.rs + Cargo.lock).

Change (display-facing):
- src-tauri/tauri.conf.json:3 productName 'txture'->'planner'
- src-tauri/tauri.conf.json:15 window title 'txture'->'planner'
- index.html:7 <title>txture</title> -> planner
- src/state/store.ts:41 agentName default signal('txture') -> 'planner'
- src/components/settings/SettingsPanel.tsx:105 fallback || 'txture' -> 'planner'
- README.md:1 / TODO.md:1 headings (optional, cosmetic)

Leave as-is unless asked (would break data dir / build identity):
- tauri.conf.json:5 identifier com.heyo.txture
- src-tauri/Cargo.toml name/lib name, Cargo.lock:5064, src-tauri/src/main.rs:5 txture_lib::run(), package.json name, agent/package.json name (npm pkg names, not user-facing).

Verify: rg -n 'txture' to confirm only intended user-facing strings changed; build app; window title + in-app name show 'planner'. Note: settings may persist old vm_name in saved config — confirm fresh default shows 'planner'.
- 2026-05-29T03:42:10Z [sarocu] ADDENDUM — marketing/index.html also contains 'txture' (verify with: rg -n txture marketing/index.html). Decide explicitly: in-scope if rename should cover the marketing page copy/title; out-of-scope if task is strictly the app's runtime display name. Default: treat marketing site as separate, leave unless user wants the marketing page renamed too. Note in PR which choice was made.
- 2026-05-29T03:52:26Z [sarocu] DONE — renamed user-facing app/product name txture -> planner.
Changed (display-facing only): src-tauri/tauri.conf.json productName + window title; index.html <title>; src/state/store.ts agentName signal default; src/components/settings/SettingsPanel.tsx vm_name fallback.
Left intentionally (per plan, would change build/data-dir/pkg identity): tauri.conf identifier com.heyo.txture, package.json name 'txture', agent/package.json 'txture-agent', Cargo crate.
NOTE: the in-app <h1 class=app-title> binds to agentName, which SettingsPanel load (line 92) overwrites with config.vm_name (default 'todo-agent'). So that header shows the agent/sandbox name, not the app name — that's the agent identity (chosen via StatusPopover), a separate concept from the app product name. The OS window title, taskbar, and browser tab now read 'planner'. The store default/fallback only surface when vm_name is empty.
Marketing site (marketing/index.html) left unchanged — out of scope per plan addendum (separate site).
Verify: tsc --noEmit clean; vite build clean. Click-test not possible (no wayland socket; same env limit as T-002).
