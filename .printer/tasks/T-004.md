+++
id = "T-004"
title = 'On the "week" view, instead of a pill with the number of tasks, have a simple meter with bars indicating the number of tasks; under 4 is green, 4-6 yellow, 7+ red (think video game power meters)'
status = "done"
priority = 3
created_at = "2026-05-29T03:38:09Z"
updated_at = "2026-05-29T03:53:02Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "a02fbee9921c2a09"
+++
## Notes

- 2026-05-29T03:40:59Z [sarocu] PLAN — week view: replace count pill with bar power meter
File: src/components/days/DayAccordion.tsx (WeekAccordion). CSS: src/theme/global.css (.accordion-badge:279-289).

Steps:
1. DayAccordion.tsx:135 replace {count > 0 && <span class=accordion-badge>{count}</span>} with a meter component/markup. count already computed at :122 (todos.length).
2. Render N small segment bars (cap at e.g. 7+ so meter doesn't grow unbounded). Color tier by count: <4 green, 4-6 yellow, 7+ red. Add level class: count<4?'low':count<7?'mid':'high'. Keep aria-label={count} for a11y; still hide when count===0.
   Markup: <span class=task-meter data-level=low|mid|high>{Array.from({length: Math.min(count,7)}).map(()=><span class=task-meter-bar/>)}</span>
3. CSS in global.css near .accordion-badge: .task-meter (flex, gap:2px, align end), .task-meter-bar (width 3px, height 8px or graduated heights for game-meter look, border-radius 1px), color via [data-level=low] .task-meter-bar{background:var(--ok/green)}, =mid{yellow}, =high{red}. Use existing theme color vars if present (check --color-* in global.css) else add --color-meter-low/mid/high.
4. Optionally graduate bar heights so it reads like a power meter (bars increase in height left->right).

Verify: build clean; run app week view; days with 1-3 todos green, 4-6 yellow, 7+ red; 0 todos no meter; today/selected styling unaffected. Confirm thresholds: 3=green,4=yellow,6=yellow,7=red.
- 2026-05-29T03:53:02Z [sarocu] DONE — week-view count pill replaced with power meter.
- src/components/days/DayAccordion.tsx: count>0 now renders <span class=task-meter data-level=low|mid|high> with Math.min(count,7) <span class=task-meter-bar/> segments. Level: count<4 low, 4-6 mid, 7+ high. role=img + aria-label/title '<n> task(s)' for a11y. Hidden at count===0 (unchanged).
- src/theme/global.css: added .task-meter (inline-flex, align-end, gap 2px), .task-meter-bar (3px wide, graduated heights 5->14px left->right for the game-meter look), and data-level color tiers via --color-meter-low/mid/high vars with green/yellow/red fallbacks (#3fb950/#d29922/#f85149).
Thresholds confirmed: 3=green, 4=yellow, 6=yellow, 7=red.
Verify: tsc --noEmit clean; vite build clean. Click-test not possible (no wayland socket; same env limit).
