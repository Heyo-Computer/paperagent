+++
id = "T-005"
title = 'On the "month" view, instead of a dot indicating events, make it a heat map'
status = "done"
priority = 3
created_at = "2026-05-29T03:38:09Z"
updated_at = "2026-05-29T03:54:07Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "3788700dec607b60"
+++
## Notes

- 2026-05-29T03:41:10Z [sarocu] PLAN — month view: replace event dot with heat map
File: src/components/days/MonthAccordion.tsx (MonthAccordion). CSS: src/theme/global.css (.cal-dot:381-388, .cal-cell, .cal-selected .cal-dot:373-375).

Steps:
1. MonthAccordion.tsx:93 count already = entry?.todos?.length ?? 0 per cell.
2. Remove the dot at :105 ({count>0 && <span class=cal-dot/>}). Instead apply a heat intensity to the cell background. Compute a bucket from count: 0 none, 1-2 heat-1, 3-4 heat-2, 5-6 heat-3, 7+ heat-4 (tune). Add to cell class:  (only when inRange && count>0) on the <button class=cal-cell ...> at :100.
   Alt: set style background via a single accent color at increasing alpha (data-driven) to avoid many CSS classes: style={count>0?{'--heat':opacityForCount(count)}:undefined} and CSS background: color-mix or rgba(var(--color-accent-rgb), var(--heat)). Prefer discrete classes for predictability.
3. CSS: add .cal-cell.heat-1..heat-4 { background: rgba(accent, .15/.35/.6/.85) } (or green->red gradient if a calendar-heat semantics preferred — confirm desired palette; spec just says heat map, use accent-intensity ramp). Ensure cal-today / cal-selected outlines still visible over heat bg (keep border/outline, layer heat behind). Keep cal-day-num legible on dark heat (use color-mix or text shadow on high buckets).
4. Remove now-unused .cal-dot rules (381-388, 373-375) once dot gone — or keep if used elsewhere (rg cal-dot first).

Verify: build clean; run app month view; empty days plain, busy days progressively hotter; today + selected cell still distinguishable; out-of-range (cal-out) cells unaffected; day numbers readable across all buckets.
- 2026-05-29T03:41:20Z [sarocu] CORRECTION to step 2 (prev line dropped a token): add the heat bucket to the cell class with a template literal — class={"cal-cell" + (inRange && count>0 ? " heat-" + bucket : "") + ...} where bucket is 1..4 computed from count.
- 2026-05-29T03:54:07Z [sarocu] DONE — month-view event dot replaced with heat map.
- src/components/days/MonthAccordion.tsx: removed <span class=cal-dot/>. Cell now gets a heat-N class (bucket: 1-2->1, 3-4->2, 5-6->3, 7+->4) when inRange && count>0; class string appends ${heatClass}. Added title='<n> task(s)' for hover/a11y.
- src/theme/global.css: removed .cal-dot and .cal-selected .cal-dot rules. Added .cal-cell.heat-1..4 backgrounds via color-mix(in srgb, var(--color-accent) 18/38/62/88%, transparent). heat-3/4 flip .cal-day-num to --color-accent-text for legibility on dark tints; .cal-day-num gets position:relative+z-index:1 to sit above bg. .cal-today now uses an inset accent outline (kept visible over heat). Re-added .cal-cell.cal-selected after the heat rules so selection background wins over heat. cal-out/cal-empty unaffected.
Verify: tsc --noEmit clean; vite build clean. Click-test not possible (no wayland socket; same env limit).
