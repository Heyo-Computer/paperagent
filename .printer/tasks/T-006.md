+++
id = "T-006"
title = 'Add two tables "lists" and "books"'
status = "done"
priority = 3
created_at = "2026-05-29T17:57:20Z"
updated_at = "2026-05-29T18:05:03Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "837d0870fe045453"
+++
## Notes

- 2026-05-29T18:00:05Z [sarocu] PLAN — Data layer foundation for lists & books (JSON-file store, mirrored across all 3 layers). No UI in this task.

Storage layout (mirror backlog.json pattern, root = state.storage_root locally / /data/storage in agent):
- Lists: storage/lists/index.json (array of {id,name,updated_at}) + storage/lists/{listId}.json (full List w/ fields + items).
- Books: storage/books/index.json + storage/books/{bookId}/book.json (meta + page TOC) + storage/books/{bookId}/pages/{pageId}.md (page body). Splits md body from metadata exactly like day.json + specs/{id}.md.

Rust types — new src-tauri/src/models/list.rs + book.rs, register in models/mod.rs:
- ListField { key:String, label:String, kind:FieldKind (enum Text|Number|Date|Bool|Select), options:Option<Vec<String>> }
- ListItem { id, values:serde_json::Map<String,Value>, linked_todos:Vec<TodoRef>, created_at, updated_at }
- List { id, name, fields:Vec<ListField>, items:Vec<ListItem>, created_at, updated_at }
- BookPage { id, title, order:i64, linked_todos:Vec<TodoRef>, created_at, updated_at } (body lives in .md file, not struct)
- Book { id, name, pages:Vec<BookPage>, created_at, updated_at }
- TodoRef { kind, id, ... } — shared link ref struct (used fully in T-009).
- All structs: #[derive(Serialize,Deserialize,Clone)], #[serde(default)] on new/optional fields for back-compat.

Rust persistence — add to src-tauri/src/services/storage.rs (primitives only; CRUD lands in T-007/008):
- lists_root()/books_root() path helpers; load_lists_index/save; load_list/save_list; load_books_index/save; load_book/save_book; load_page/save_page(md); ensure_*_dir. Match existing load_backlog/save_backlog error style (Result<_,String>).

Agent TS mirror — new agent/src/tools/lists.ts + books.ts:
- Re-implement the same load/save primitives over /data/storage/lists & /data/storage/books using node:fs (copy structure from agent/src/tools/todo.ts: loadBacklogFile/saveBacklogFile shape, randomUUID, mkdirSync recursive).

Verify:
- cd src-tauri && cargo check (types compile, no handler wiring yet).
- cd agent && bun run build (or tsc --noEmit) — TS compiles.
- Manual: none (no commands/UI yet); confirmed by T-007/008.
- 2026-05-29T18:05:03Z [sarocu] DONE — data-layer foundation across Rust + agent TS.

Rust: new models/link.rs (TodoRef, LinkRef), models/list.rs (FieldKind enum, ListField, ListItem, List, ListSummary), models/book.rs (BookPage, Book, BookSummary); registered in models/mod.rs. services/storage.rs: Lists + Books primitives (root/ensure-dir, load/save index, load/save/delete entity, load/save/delete page md, upsert/remove index sorted by name). All #[serde(default)], Result<_,String> style.

Agent TS: new agent/src/tools/lists.ts + books.ts mirroring the same interfaces + load/save/index primitives over /data/storage/{lists,books}.

Layout: lists/index.json + lists/{id}.json ; books/index.json + books/{id}/book.json + books/{id}/pages/{pageId}.md.

CRUD/commands/RPC/UI deferred to T-007/008 per plan.

Verify: cargo check clean (pre-existing warnings only); agent tsc exit 0 (emits dist/tools/lists.js + books.js). No UI in this task → no click-test.
