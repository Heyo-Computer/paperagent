+++
id = "T-007"
title = "A list is a collection of items, e.g. customers, the attributes should be specific to the list"
status = "done"
priority = 3
created_at = "2026-05-29T17:57:20Z"
updated_at = "2026-05-29T18:12:59Z"
owner = "sarocu"
labels = []
depends_on = ["T-006"]
blocked_reason = ""
spec_anchor = "7713aaade69fbcb9"
+++
## Notes

- 2026-05-29T18:00:20Z [sarocu] PLAN — Lists feature (a list = collection of items with list-specific attributes/schema). Depends on T-006 types/persistence. Mirror the backlog vertical end-to-end.

Backend CRUD (src-tauri/src/services/storage.rs + agent/src/tools/lists.ts, keep both in sync):
- create_list(name, fields:Vec<ListField>) -> List
- list_lists() -> Vec<ListSummary>  (from index.json)
- load_list(id) -> List
- update_list_meta(id, name, fields) -> List  (schema edits; on field removal drop that key from item.values)
- delete_list(id)  (remove {id}.json + index entry)
- add_list_item(listId, values) -> List ; update_list_item(listId, item) ; delete_list_item(listId, itemId)
- Stamp created_at/updated_at via chrono (Rust) / new Date().toISOString() (TS), uuid/randomUUID for ids.

Tauri commands — new src-tauri/src/commands/lists.rs:
- One #[tauri::command] per fn above. Copy the agent-routing pattern from commands/storage.rs verbatim: agent_url(&state) -> agent_rpc(url, "lists/<op>", json!{...}) else call svc::. 
- Register: add `pub mod lists;` to commands/mod.rs; add every command to the generate_handler! list in lib.rs (new "// Lists" block).

Agent RPC — agent/src/index.ts: add `lists/create_list`, `lists/list_lists`, `lists/load_list`, `lists/update_list_meta`, `lists/delete_list`, `lists/add_list_item`, `lists/update_list_item`, `lists/delete_list_item` cases in the switch, dispatching to lists.ts (mirror the existing storage/* cases).

Frontend:
- src/types.ts: add List, ListField, FieldKind, ListItem, ListSummary interfaces (match Rust serde shape, snake_case fields).
- src/api/commands.ts: invoke wrappers for each command (new "// Lists" block).
- src/types.ts ViewTab: add "lists". src/App.tsx: add {id:"lists",label:"Lists"} to tabs and render <ListsPanel/>. src/state/store.ts: add lists signal + allLists (for T-009 mentions).
- New src/components/lists/ListsPanel.tsx (model on BacklogPanel.tsx): left = pick/create list (create form has a field-schema editor: add rows of label+kind[+select options]); right = table whose columns come from list.fields, rows = items, inline add/edit/delete cell values per field kind. reload() on mount; optimistic setState from command return values.

Verify:
- cd src-tauri && cargo check ; cd agent && bun run build ; bun run build (vite tsc).
- Manual (bun run tauri dev): create list "customers" with fields name(text)/email(text)/active(bool); add an item; edit a cell; delete; reload app → persists (storage/lists/{id}.json on disk).
- 2026-05-29T18:12:59Z [sarocu] DONE — Lists feature wired end-to-end across all 3 layers.

Backend (Rust): services/storage.rs CRUD on top of T-006 primitives — create_list, get_list, list_lists, update_list_meta (drops removed-field keys from every item.values), delete_list, add_list_item, update_list_item, delete_list_item. New commands/lists.rs: one #[tauri::command] each with the agent_url/agent_rpc routing pattern copied from commands/storage.rs (RPC method 'lists/<op>'). Registered pub mod lists in commands/mod.rs and all 8 commands in lib.rs (// Lists block).

Agent: agent/src/tools/lists.ts CRUD (listLists/getList/createList/updateListMeta/deleteList/add/update/deleteListItem). agent/src/index.ts: 8 'lists/*' RPC cases dispatching to lists.ts.

Frontend: types.ts (FieldKind, ListField, TodoRef, ListItem, List, ListSummary; ViewTab += 'lists'). api/commands.ts (// Lists invoke wrappers). state/store.ts (allLists signal, ListSummary import). App.tsx (Lists tab + <ListsPanel/> + boot-load allLists via listLists()). New components/lists/ListsPanel.tsx: left rail picks/creates lists with a field-schema editor (label + kind[text|number|date|bool|select] + comma options for select); right pane renders a table from list.fields with per-kind inline cell editors, optimistic update on edit, add-row + delete-row, plus Edit-schema and Delete-list actions. CSS added to theme/global.css (.lists-* + .list-cell-input).

Verify: cd src-tauri && cargo check → clean. agent npx tsc → exit 0. Frontend npx tsc --noEmit + vite build → exit 0 (built dist). Rebuilt debug binary (cargo build) and smoke-launched ./target/debug/planner → process boots and stays alive (only env GPU/MESA warnings, no crash).

Functional test: exercised the compiled lists.js CRUD against a temp root — create list 'Customers' w/ name/email/active(bool), add item {Acme,...}, edit email cell, schema-edit dropping 'email' field correctly strips it from item.values, index reflects rename, delete item, delete list → index empties. On-disk layout confirmed: storage/lists/index.json + storage/lists/{id}.json.

CLICK-TEST LIMITATION: WAYLAND_DISPLAY is set (wayland-1) but the 'computer' automation binary is not built/installed (only its SKILL.md ships under ~/.printer/plugins/computer; no source/binary), and no grim/xdotool fallback exists — so an in-app click-through of the Lists tab could not be performed. Compensated with the binary smoke-launch + the functional CRUD test above.
