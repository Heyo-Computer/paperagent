+++
id = "T-008"
title = "a book is a collection of markdown documents; similar to a notes style app with organization and a table of contents"
status = "done"
priority = 3
created_at = "2026-05-29T17:57:20Z"
updated_at = "2026-05-29T18:26:28Z"
owner = "sarocu"
labels = []
depends_on = ["T-006"]
blocked_reason = ""
spec_anchor = "87981a98c8060eae"
+++
## Notes

- 2026-05-29T18:00:32Z [sarocu] PLAN — Books feature (a book = collection of markdown docs with organization + table of contents). Depends on T-006. Reuse the existing markdown editor from T-002.

First, locate the reusable editor: read src/components/todos/TodoSpec.tsx + src/components/markdown/MarkdownRenderer.tsx to identify the edit/preview component (file-type aware editor added in T-002) and reuse it for page bodies rather than building a new one.

Backend CRUD (src-tauri/src/services/storage.rs + agent/src/tools/books.ts, in sync):
- create_book(name) -> Book ; list_books() -> Vec<BookSummary> ; load_book(id) -> Book (meta + ordered TOC)
- delete_book(id)
- add_page(bookId, title) -> Book  (append with order = max+1; create empty pages/{pageId}.md)
- load_page(bookId, pageId) -> String (md) ; save_page(bookId, pageId, content) (writes .md, bumps page.updated_at)
- update_page_meta(bookId, pageId, title) ; reorder_pages(bookId, orderedIds) ; delete_page(bookId, pageId) (drop .md + TOC entry)

Tauri commands — new src-tauri/src/commands/books.rs: one command per fn, agent-routing pattern from commands/storage.rs (agent_rpc "books/<op>"). Register `pub mod books;` in commands/mod.rs + all commands in lib.rs handler ("// Books" block).

Agent RPC — agent/src/index.ts: add `books/*` cases mirroring the ops above, dispatching to books.ts.

Frontend:
- src/types.ts: Book, BookPage, BookSummary interfaces. ViewTab += "books".
- src/api/commands.ts: invoke wrappers ("// Books" block).
- src/App.tsx: tab {id:"books",label:"Books"} + <BooksPanel/>. store.ts: books signal + allBooks (T-009).
- New src/components/books/BooksPanel.tsx: 3-pane — book picker/create, TOC sidebar (pages in order, add-page, drag/buttons to reorder, rename, delete), main pane = reused markdown editor bound to load_page/save_page (debounced or save-on-blur).

Verify:
- cargo check ; agent bun run build ; vite build.
- Manual (tauri dev): create book "standup", add page, type markdown, switch pages (persists via save_page), rename + reorder TOC, reload app → intact (storage/books/{id}/book.json + pages/*.md on disk).
- 2026-05-29T18:26:23Z [sarocu] DONE — Books feature wired end-to-end across all 3 layers (Rust storage CRUD already existed from T-006).

Backend (Rust): new commands/books.rs — 10 #[tauri::command]s (list_books, load_book, create_book, delete_book, add_page, load_page, save_page, update_page_meta, reorder_pages, delete_page) using the agent_url/agent_rpc routing pattern (method 'books/<op>') copied from commands/lists.rs. Registered pub mod books in commands/mod.rs and all 10 commands in lib.rs (// Books block). load_page wraps the non-Result svc::load_page in Ok().

Agent: agent/src/tools/books.ts — added CRUD on top of the existing primitives (listBooks/getBook/createBook/deleteBook/addPage/savePageContent/updatePageMeta/reorderPages/deletePage), imported randomUUID. agent/src/index.ts: 10 'books/*' RPC cases dispatching to books.js.

Frontend: types.ts (BookPage, Book, BookSummary; ViewTab += 'books'). api/commands.ts (// Books invoke wrappers). state/store.ts (allBooks signal + BookSummary import). App.tsx (Books tab + <BooksPanel/> + boot-load allBooks via listBooks()). New components/books/BooksPanel.tsx: 3-pane — left rail picks/creates books; middle TOC sidebar (pages in order, add-page inline, rename-in-place, up/down reorder, delete) ; main pane reuses the T-002 MarkdownRenderer with a click-to-edit textarea bound to load_page/save_page. CSS (.books-*) added to theme/global.css.

Verify: cd src-tauri && cargo check -> clean (pre-existing warnings only). agent npx tsc -> exit 0, emits dist/tools/books.js + 10 books/ RPC cases in dist/index.js. Frontend npx tsc --noEmit + vite build -> exit 0.

Functional test: exercised compiled dist/tools/books.js against writable /data/storage — createBook('standup'), addPage x2, savePageContent + loadPage round-trip, updatePageMeta (rename), reorderPages (verified order flip), deletePage, deleteBook; index.json reflected each step and emptied on delete. All correct.

Click-test (X11 session, DISPLAY=:1; computer CLI unusable since no Wayland socket, used xdotool + ImageMagick import): built+launched ./target/debug/planner against a vite dev server on :1420. Confirmed via screenshots: Books tab appears in nav, clicking it renders BooksPanel (left rail, '+ New', 'No books yet' empty state, main pane prompt); '+ New' opens the New-book form; typed 'Standup notes' into the name field successfully. The final create write FAILS only because the app's storage_root (~/.todo/storage) sits on a sandbox READ-ONLY ext4 mount (ro,errors=remount-ro; remount denied, app runs as root) — this blocks ALL persistence equally (todos/lists/books) and is an env constraint, not a code defect. Disk-level CRUD correctness is covered by the /data/storage functional test above.
