+++
id = "T-009"
title = "both lists and books can link to, and be linked from to-dos"
status = "done"
priority = 3
created_at = "2026-05-29T17:57:20Z"
updated_at = "2026-05-29T19:08:38Z"
owner = "sarocu"
labels = []
depends_on = ["T-007", "T-008", "T-012", "T-013"]
blocked_reason = ""
spec_anchor = "b1f4b00d7f019a2b"
+++
## Notes

- 2026-05-29T18:00:48Z [sarocu] PLAN — Bidirectional links between todos and lists/books. Depends on T-007, T-008. Extends the existing @-mention token system.

Token grammar — src/components/markdown/mentions.tsx:
- Extend MENTION_RE (currently todo `id:..|date:..` and `artifact:..`) with two alts: `list:<listId>/<itemId>` and `book:<bookId>/<pageId>`. Keep the single-capture-group-per-kind structure.
- Update mentionsToHtml + MentionText to emit chips: span.mention-list[data-list-id,data-item-id] and span.mention-book[data-book-id,data-page-id]. Add matching CSS classes alongside .mention-artifact.

Mention autocomplete — src/components/chat/ChatInput.tsx:
- Extend MentionOption union with {kind:"list",...} and {kind:"book",...}. In getMentionOptions(), iterate allLists/allBooks signals (populate them at boot in App.tsx useEffect, same as allArtifacts via listAllArtifacts). Build tokens in insertMention for list items / book pages. Add mention-kind labels.

Data model (the "linked-from" direction) — T-006 already added linked_todos:Vec<TodoRef> on ListItem/BookPage. Now add the reverse on the todo:
- models/todo.rs TodoItem: add `links:Vec<LinkRef>` (#[serde(default)]). LinkRef { kind:"list"|"book", target_id, sub_id, label }. Mirror in src/types.ts TodoItem + agent/src/tools/todo.ts interface (+ backfill in load like has_spec).
- Add link/unlink ops kept consistent on BOTH sides: link_todo_to_list_item / link_todo_to_book_page (writes LinkRef onto the todo AND TodoRef onto the item/page), plus unlink. Implement in services/storage.rs + agent/src/tools, expose as Tauri commands (commands/storage.rs or new commands/links.rs; register in lib.rs) + agent RPC `links/*` in index.ts.

Rendering the links:
- TodoItem.tsx / TodoSpec.tsx: render linked lists/books as clickable chips that switch tab + select target.
- ListsPanel / BooksPanel: show linked todos on an item/page.

Agent awareness: update system prompt in agent/src/agent.ts to document the list:/book: token forms (next to the existing @[..](id:..) / artifact: docs).

Verify:
- cargo check ; agent build ; vite build.
- Manual: @-mention a list item and a book page in chat → render as chips; create a link from a todo → appears on both the todo and the item/page; reload → persists both directions.

NOTE: this is broad. If it cannot land in one pass, split into T-009a (token grammar + autocomplete + rendering) and T-009b (link data model + commands/RPC + bidirectional persistence) and depend accordingly.
- 2026-05-29T18:28:59Z [sarocu] Split into T-012 (T-009b: link data model + bidirectional ops + commands/RPC + link rendering) and T-013 (T-009a: mention token grammar + autocomplete + chip rendering). T-009 now depends on both; T-011 re-pointed to depend on T-012. Verified against code: link.rs already defines TodoRef+LinkRef and ListItem/BookPage already carry linked_todos (from T-006/008); TodoItem lacks links, no commands/links.rs, mentions.tsx handles only todo+artifact.
- 2026-05-29T19:08:38Z [sarocu] Closing umbrella — both sub-tasks complete: T-012 (link data model + bidirectional ops + commands/RPC + link rendering) and T-013 (mention token grammar list:/book: + autocomplete + chip rendering/nav). Bidirectional todo<->list/book linking is fully implemented.
