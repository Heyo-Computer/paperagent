+++
id = "T-010"
title = "the agent needs new tools to work with lists and books"
status = "done"
priority = 3
created_at = "2026-05-29T17:57:20Z"
updated_at = "2026-05-29T18:46:32Z"
owner = "sarocu"
labels = []
depends_on = ["T-006", "T-007", "T-008"]
blocked_reason = ""
spec_anchor = "3c6c46e97ff74c59"
+++
## Notes

- 2026-05-29T18:01:00Z [sarocu] PLAN — Agent tools for lists & books. Depends on T-006/007/008 (reuses their TS functions; single source of truth). All edits in agent/.

agent/src/agent.ts — add to the `tools: ToolSchema[]` array (mirror existing get_backlog/add_backlog_item schemas):
- list_lists() ; get_list(list_id) ; create_list(name, fields[]) ; add_list_item(list_id, values) ; update_list_item(list_id, item_id, values) ; delete_list_item(list_id, item_id)
- list_books() ; get_book(book_id) (returns TOC) ; create_book(name) ; add_book_page(book_id, title, content?) ; get_book_page(book_id, page_id) ; update_book_page(book_id, page_id, title?, content?)
- (link tools land with T-009: link_todo_to_list_item / link_todo_to_book_page)
Keep descriptions action-oriented; mark required params.

agent/src/agent.ts executeTool() switch — add a case per tool calling the imported fns from ./tools/lists.js and ./tools/books.js (created in T-006/007/008). Return JSON.stringify(result) or a human summary string, matching existing cases.

System prompt (same file): extend the storage-structure block to document:
  /data/storage/lists/{list-id}.json — a list (fields + items)
  /data/storage/books/{book-id}/book.json + pages/{page-id}.md — a book and its pages
and a line telling the agent to prefer list_*/book_* tools over raw write_file (same guidance pattern as save_artifact vs write_file).

No new RPC needed here — chat tools are in-process; the lists/* + books/* RPCs are added in T-007/008 for the Tauri host.

Verify:
- cd agent && bun run build (or tsc --noEmit) compiles; import paths resolve.
- Manual (tauri dev, agent running): chat "create a customers list with name and email", "add Acme to it", "make a book called standup and add today page" → tools fire, files appear under /data/storage, UI tabs reflect them after refresh.
- 2026-05-29T18:46:32Z [sarocu] Added 12 in-process agent tools (list_lists/get_list/create_list/add_list_item/update_list_item/delete_list_item, list_books/get_book/create_book/add_book_page/get_book_page/update_book_page) in agent/src/agent.ts: schemas, executeTool cases, system-prompt storage+guidance block. Widened ToolSchema property type in providers/types.ts to allow nested items/enum/properties. tsc --noEmit clean. Verified end-to-end by driving the real executeTool switch against compiled dist for every new tool; all persisted under /data/storage/lists and /data/storage/books per documented layout. NOTE: ANTHROPIC/OPENROUTER keys unset, so live tauri+LLM chat click-test not run; verified via the executeTool routing layer instead.
