+++
id = "T-012"
title = "T-009b: link data model + bidirectional ops + commands/RPC + link rendering"
status = "done"
priority = 3
created_at = "2026-05-29T18:28:53Z"
updated_at = "2026-05-29T19:01:12Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = ""
+++
Add links:Vec<LinkRef> to TodoItem (rust/ts/agent). Bidirectional link/unlink ops in services/storage.rs + agent tools, kept consistent on both sides (LinkRef on todo, TodoRef on item/page). Expose Tauri commands (commands/links.rs, register in lib.rs) + agent RPC links/*. Render link chips on TodoItem/TodoSpec and linked todos on ListsPanel/BooksPanel.

## Notes

- 2026-05-29T18:48:21Z [sarocu] PLAN — remaining work (backend already landed):
- DONE: links:Vec<LinkRef> on TodoItem (rust models/todo.rs, src/types.ts:1-16, agent/src/tools/todo.ts); link/unlink_todo_to_list_item/book_page in services/storage.rs:712-860; commands/links.rs (NEW, untracked) registered lib.rs:151-154; agent RPC links/* index.ts:309-327.
- TODO render link chips on the todo:
  * TodoItem.tsx (46 lines) renders title+expand only; no links shown. Add a chip row under .todo-item-row when todo.links?.length, one chip per LinkRef (label||sub_id) with kind class. Click sets activeTab + pendingListSelection/pendingBookSelection (store.ts:44-45).
  * TodoSpec.tsx (174 lines): same chips inside the expanded spec, plus an unlink (x) calling unlink_todo_from_* command + onUpdate.
- TODO linked-todos on panels:
  * ListsPanel.tsx (376) / BooksPanel.tsx (316): when an item/page is selected, render its linked_todos (TodoRef[]) as chips that navigate to day tab + expandedTodoId.
- Need api/commands.ts wrappers for the 4 link/unlink Tauri commands if not yet added — grep before adding.
- Verify: cargo check; pnpm -C agent build; vite build; manual link via agent → chip on both todo and item/page, reload persists both directions.
- 2026-05-29T19:01:09Z [sarocu] DONE — frontend link rendering + navigation landed; backend verified by tests.
- store.ts: added pendingTodoSelection signal + navigateToLink(LinkRef) / navigateToTodo(TodoRef) helpers (set activeTab + pending*Selection).
- TodoItem.tsx: renders link chips (mention mention-list/-book) under the row when collapsed; click navigates to target tab+selection.
- TodoSpec.tsx: renders link chips inside expanded spec with an unlink (✕) calling unlink_todo_from_list_item/book_page + onUpdate to drop the LinkRef.
- ListsPanel.tsx: new 'Linked todos' column rendering item.linked_todos as chips (navigateToTodo); consumes pendingListSelection to open+highlight the list/row.
- BooksPanel.tsx: renders selectedPage.linked_todos chips under the page header; consumes pendingBookSelection to open book+page.
- DayPanel.tsx: consumes pendingTodoSelection (switches viewedDate + expands the todo after load).
- global.css: .mention-list/.mention-book + chip/linked-todo/highlight styles.
Verify: tsc --noEmit clean; vite build OK; cargo check OK; agent tsc clean. Added storage.rs link_tests (list+book) asserting bidirectional persistence across reload incl. unlink — both pass (cargo test --lib link_tests: 2 passed).
Click-test NOT performed: $WAYLAND_DISPLAY is set but no compositor socket is reachable by the 'computer' tool (XDG_RUNTIME_DIR has no wayland-1 socket) and the GL stack errors out (amdgpu/MESA software-fallback). App binary launches without panic against a seeded data dir and loads the updated frontend via vite devUrl, but the window cannot be screenshotted/driven here.
