+++
id = "T-013"
title = "T-009a: mention token grammar (list:/book:) + autocomplete + chip rendering"
status = "done"
priority = 3
created_at = "2026-05-29T18:28:53Z"
updated_at = "2026-05-29T19:08:28Z"
owner = "sarocu"
labels = []
depends_on = ["T-012"]
blocked_reason = ""
spec_anchor = ""
+++
Extend MENTION_RE in src/components/markdown/mentions.tsx with list:<listId>/<itemId> and book:<bookId>/<pageId> alts; emit mention-list/mention-book chips (+CSS). Extend ChatInput.tsx MentionOption union + getMentionOptions to offer list items/book pages (allLists/allBooks signals populated at boot in App.tsx). Chips navigate to the target tab+selection on click. Document token forms in agent system prompt.

## Notes

- 2026-05-29T18:48:11Z [sarocu] PLAN (grounded against current code) —

Token grammar — src/components/markdown/mentions.tsx:
- MENTION_RE today (line 8) has 2 alts (todo id|date, artifact). Add two more alts: list:<listId>/<itemId> and book:<bookId>/<pageId>. New capture groups 5/6 (list) and 7/8 (book). Update the header comment listing token forms.
- mentionsToHtml (line 22) + MentionText (line 33): add branches emitting span.mention.mention-list[data-list-id,data-item-id] and span.mention.mention-book[data-book-id,data-page-id]. escapeHtml ids in the HTML path. Mirror the same branch order in both functions.
- CSS: add .mention-list / .mention-book next to .mention-artifact (find the artifact rule in styles).

Autocomplete — src/components/chat/ChatInput.tsx:
- allLists/allBooks signals ALREADY populated at boot (App.tsx:53-54 via listLists/listBooks). Import them (line 2 currently imports days, allArtifacts).
- MentionOption union (line 32): add {kind:'list',label,sub,token} and {kind:'book',...}. getMentionOptions (line 172): after artifacts loop (line 183), iterate allLists.value items and allBooks.value pages, filtering by query; build token list:<listId>/<itemId> and book:<bookId>/<pageId>. NOTE: ListSummary/BookSummary may not carry item/page detail — confirm shape; if summaries lack children, mention only the list/book root or fetch detail. mention-kind label classes (line 348) already keyed by opt.kind, add CSS for -list/-book.

Chip click navigation (NEW — chips currently have NO click handler):
- Navigation infra already exists: activeTab signal + pendingListSelection / pendingBookSelection signals (store.ts:44-45). On mention-list/mention-book chip click set activeTab='lists'|'books' and pendingListSelection={listId,itemId} / pendingBookSelection={bookId,pageId}. Add a delegated click handler where chips render (MarkdownRenderer output + MentionText) reading data-* attrs.

Agent prompt: document list:/book: token forms in agent/src/agent.ts next to existing @[..](id:..)/artifact: docs.

Verify: vite build; manual — @-mention a list item + book page in chat render as chips; click chip navigates+selects; reload persists.
- 2026-05-29T18:49:41Z [sarocu] GROUNDING (verified) — plan accurate; two path corrections: (1) store signals live at src/state/store.ts NOT src/store.ts — pendingListSelection (listId/itemId?) at :44, pendingBookSelection (bookId/pageId?) at :45, activeTab at :5. (2) mentions.tsx confirmed: MENTION_RE at line 8-9 (4 groups: label, todoId, todoDate, artifactPath), mentionsToHtml at 22, MentionText at 33 — extend both with list/book branches. No chip click handler exists today; add delegated handler reading data-* to set activeTab + pending*Selection.
- 2026-05-29T19:02:37Z [sarocu] GROUNDING v2 (data-shape resolved) — confirmed ListSummary/BookSummary = {id,name,updated_at} ONLY (src/types.ts:64,87); no items/pages. allLists/allBooks signals (store.ts:37,40) hold summaries. ChatInput.tsx imports only {days, allArtifacts} (line 2); MentionOption union at :32-34; getMentionOptions at :172 (pushes todos then artifacts); insertMention token build at :204-206.

DECISION for one-pass scope:
- Token grammar PARSES both root and sub forms: list:<listId>(/<itemId>)? and book:<bookId>(/<pageId>)? — sub-id OPTIONAL. So agent/T-011-generated links carrying /itemId render correctly; store nav already treats itemId?/pageId? as optional (store.ts:44-45, navigateToLink :52).
- Autocomplete offers list/book ROOTS only (token list:<listId> / book:<bookId>, no sub-id) — matches summary signals without N per-candidate detail fetches. Item/page-level mention autocomplete is explicitly OUT of this pass (would need full list/book load); note this limitation in the task on completion.

Concrete steps:
1. mentions.tsx: MENTION_RE add 2 alts after artifact -> groups 5(listId),6(itemId),7(bookId),8(pageId). Update header comment + both mentionsToHtml(:22) and MentionText(:33) with list/book branches emitting span.mention.mention-list[data-list-id,data-item-id] / .mention-book[data-book-id,data-page-id]; escapeHtml ids in HTML path; same branch order both fns.
2. ChatInput.tsx: import allLists, allBooks (line 2). Extend MentionOption union (:34) with {kind:'list';key;label;sub;listId} and {kind:'book';...;bookId}. In getMentionOptions (:172) after artifacts loop, push allLists.value (label=name, sub='list', token base list:<id>) and allBooks.value filtered by query. insertMention (:204): add list/book token branches (no sub-id).
3. Chip click nav: add delegated click handler where chips render (MarkdownRenderer output + MentionText container) reading data-list-id/data-book-id -> call navigateToLink-equiv (set activeTab + pending*Selection from store.ts). MentionText currently returns inert spans; wrap container with onClick delegation.
4. CSS: .mention-list/.mention-book already added by T-012 in global.css — verify present, add mention-kind label classes for autocomplete rows (:348 area keyed by opt.kind).
5. Agent prompt (agent/src/agent.ts): document list:/book: token forms next to @[..](id:..)/artifact:.
Verify: pnpm tsc --noEmit; vite build; manual @-mention a list + book in chat -> chips; click chip -> switches tab + selects target; reload persists token text in chat history.
- 2026-05-29T19:08:25Z [sarocu] DONE — implemented:
- mentions.tsx: MENTION_RE extended with list:<id>(/<itemId>)? and book:<id>(/<pageId>)? alts (groups 5-8); mentionsToHtml + MentionText emit span.mention.mention-list[data-list-id,data-item-id] / .mention-book[data-book-id,data-page-id], same branch order (artifact->list->book->todo). Added navigateFromMention(el) helper (reads data-* off closest .mention, sets pendingListSelection/pendingBookSelection + activeTab).
- MarkdownRenderer: onClick={navigateFromMention} on container div (delegated, works with dangerouslySetInnerHTML). MessageBubble: same onClick on the user-bubble span wrapping MentionText.
- ChatInput.tsx: import allLists/allBooks; MentionOption union +list/+book; getMentionOptions offers list/book ROOTS only (token list:<id> / book:<id>, no sub-id) filtered by query; insertMention builds those tokens; mention-kind label shows list/book.
- global.css: .mention-kind-list/.mention-kind-book (autocomplete row badges) + cursor:pointer on .mention-list/.mention-book chips.
- agent/src/agent.ts: system prompt documents list:/book: token forms.

LIMITATION (per plan decision): item/page-level mention autocomplete is OUT of scope — summaries (ListSummary/BookSummary) carry no items/pages, so autocomplete offers roots only. The grammar still PARSES /itemId and /pageId so agent/T-011-generated links render + navigate correctly.

VERIFY: npx tsc --noEmit (frontend) = 0; npx vite build = ok; agent npx tsc = 0. Regex grammar unit-checked in node across all 4 kinds incl optional sub-ids + inline mixing (correct group capture). GUI click-test NOT run: this sandbox mounts the home FS (~/.todo storage) read-only, so no lists/books can be created/persisted and the Tauri app cannot write its data dir — $WAYLAND_DISPLAY is set but the data layer is unwritable here.
