+++
id = "T-015"
title = "Create a tool for the agent that uses the index to be able to search across everything created or managed by the app"
status = "done"
priority = 3
created_at = "2026-05-31T15:53:15Z"
updated_at = "2026-05-31T16:07:47Z"
owner = "sarocu"
labels = []
depends_on = ["T-014"]
blocked_reason = ""
spec_anchor = "5fe2d46bff42a66c"
+++
## Notes

- 2026-05-31T15:56:31Z [sarocu] PLAN (expose the T-014 index as an agent tool)

Depends on T-014 (search.ts buildIndex/searchIndex).

agent/src/agent.ts:
- Add ToolSchema `search_content` to the `tools` array:
  params { query: string (required); kinds?: string[] enum todo|backlog|spec|list|list_item|book|page|artifact; limit?: number default 10 }.
  description: "Search across everything the user has: todos, specs, backlog, lists+items, books+pages, and artifacts. Returns ranked matches; each result has kind, title, a snippet, ids, and a `token` (an @[..] mention string). When you surface a result to the user, echo its `token` verbatim so it renders as a clickable chip."
- Add `case "search_content":` in executeTool -> import { searchIndex } from ./tools/search.js; call searchIndex(input.query, { kinds: input.kinds, limit: input.limit }); JSON.stringify the array. searchIndex calls buildIndex() internally (lazy, stat-based).
- System prompt (the big template literal in chat()): add a sentence that the agent should use search_content whenever the user references something vaguely / asks "where/find/which" and that it must echo result `token`s verbatim so chips are clickable (this is what T-017 makes navigable).

Notes:
- Tool-only; no /rpc method and no Rust parity command needed (search is an agent capability, not a UI command).
- input fields arrive as strings/loose JSON (see how `fields`/`values` are cast elsewhere in executeTool) — cast kinds/limit defensively.

Verify:
- cd agent && npm run build (clean).
- Manual: start agent, send "search for <known term>", confirm the tool fires (agent.log) and returns hits spanning kinds; confirm tokens are well-formed @[..](...) strings.
- 2026-05-31T16:07:44Z [sarocu] DONE. Wired search_content agent tool in agent/src/agent.ts: import searchIndex+DocKind from ./tools/search.js; ToolSchema search_content {query(req), kinds?(enum array of 8 DocKinds), limit?(default 10)} with description telling agent to use it for vague/where/find/which refs and echo each result token verbatim for clickable chips; executeTool case with defensive kinds parse (array|JSON-array string|csv|single) + Number()-cast limit, returns JSON.stringify(searchIndex(...)); system prompt guidance + must-echo-token rule. Verify: npm run build (agent) tsc clean; seeded /data harness, searchIndex('acme') -> 4 hits across list_item/spec/page/artifact with well-formed tokens, kind filter confirmed, kinds-parse branches unit-checked. Backend-only tool, no UI surface (T-016/T-017), test seed cleaned.
