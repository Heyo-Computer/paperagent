+++
id = "T-017"
title = "We should hot link artifacts/files/todos in the chat so that when the search returns an item, clicking on it will instantly take us to the detail page for the todo/file/artifact"
status = "done"
priority = 3
created_at = "2026-05-31T15:53:15Z"
updated_at = "2026-05-31T16:18:37Z"
owner = "sarocu"
labels = []
depends_on = ["T-015"]
blocked_reason = ""
spec_anchor = "e05e1fdf7e2343e5"
+++
## Notes

- 2026-05-31T15:56:47Z [sarocu] PLAN (make every search-result chip clickable -> jump to its detail view)

Depends on T-015 (results are emitted as @[..](...) mention tokens, already rendered as chips by mentionsToHtml + MarkdownRenderer, whose container already wires onClick -> navigateFromMention).

Current state (src/components/markdown/mentions.tsx): navigateFromMention only handles list + book chips. todo chips (data-todo-id/data-todo-date) and artifact chips (data-artifact-path) are INERT. Books/Lists nav works via pendingBookSelection/pendingListSelection + activeTab (consumed in ListsPanel.tsx / BooksPanel.tsx — mirror their pending-selection effect pattern).

Changes:
1. mentions.tsx navigateFromMention: also handle
   - todo chip: read data-todo-id + data-todo-date -> if date present call navigateToTodo({date, todoId}) (store helper already sets pendingTodoSelection + activeTab=day); if date empty -> activeTab=backlog (navigateToTodo already does this when ref.date falsy).
   - artifact chip: read data-artifact-path -> new navigateToArtifact(path).
   Return true when handled.
2. src/state/store.ts: add `export const pendingArtifactSelection = signal<{ path: string } | null>(null)` and `export function navigateToArtifact(path: string){ pendingArtifactSelection.value = { path }; activeTab.value = "artifacts"; }`.
3. src/components/artifacts/ArtifactsPanel.tsx: add an effect consuming pendingArtifactSelection — split path into dir + filename, setCwd(dir), then load the file (readArtifact) and setViewing it (open the modal); clear the signal after. Mirror the pendingBook/List effect in Books/Lists panels. Guard for folder paths (just setCwd, no modal).
4. Confirm the Day view already consumes pendingTodoSelection (DayPanel/DayAccordion) and scrolls/expands to the todo; if todo chips do not land cleanly, ensure the consumer expands expandedTodoId for the target. (Backlog path: BacklogPanel — verify it surfaces the item; if not, scope a follow-up.)

Note: assistant bubbles render via MarkdownRenderer (onClick wired); user bubbles via MentionText wrapped in onClick=navigateFromMention (MessageBubble.tsx) — both paths already call navigateFromMention, so only the handler + artifact plumbing need to change.

Verify:
- npm run build (root) clean.
- Manual: run a /search that returns each kind; click each chip (todo, backlog todo, list, list item, book, book page, artifact) and confirm it opens the correct tab + detail (item/page selected, artifact modal open).
- 2026-05-31T16:04:07Z [sarocu] GROUNDING REFINEMENT (verified against code; existing plan stands, add one required pre-step):

REQUIRED 0. Loosen MENTION_RE for empty-date backlog todos. mentions.tsx:13 currently has 'id:([^|)]+)\|date:([^)]+)' — the date group requires >=1 char, so search.ts emits backlog tokens '@[t](id:<id>|date:)' that DO NOT MATCH and never render as a chip (so step 1's todo handler never fires for backlog). Fix: change that branch to 'date:([^)]*)' in MENTION_RE. The downstream code already tolerates empty date: mentionsToHtml writes data-todo-date="" (mentions.tsx:70) and navigateToTodo(store.ts:63) already routes empty date -> activeTab='backlog'. Verify no other consumer assumes non-empty date.

CONFIRMED against code:
- navigateFromMention (mentions.tsx:17-35) handles ONLY list/book today; todo (data-todo-id/data-todo-date) + artifact (data-artifact-path) chips are inert — matches plan step 1.
- pendingTodoSelection IS consumed: DayPanel.tsx:51-70 sets expandedTodoId from it and clears the signal (two effects). So dated-todo chip nav works once handler added. navigateToTodo(store.ts:63) sets pendingTodoSelection + activeTab='day' for dated, activeTab='backlog' for empty.
- BacklogPanel: confirm it surfaces/expands the item; there is no pendingBacklogSelection signal today, so empty-date todo chips will only switch to the Backlog tab (no item highlight). Acceptable for v1 — note it, don't block.
- ArtifactsPanel.tsx: has cwd(setCwd:41) + viewing(setViewing:42) + readArtifact import(:7); modal opens when viewing set & !is_dir (:273). So step 3 (pendingArtifactSelection effect: setCwd(dir) then setViewing the file) is wired exactly as planned. For a folder path, just setCwd (no modal), guard is_dir.
- store.ts: add pendingArtifactSelection signal + navigateToArtifact(path) (activeTab='artifacts') as planned (insert near :44-49 with the other pending* signals).

Verify: npm run build clean; manual /search returning each kind, click each chip (dated todo, BACKLOG todo, list, list item, book, book page, artifact) — confirm backlog-todo chip now renders AND navigates.
- 2026-05-31T16:15:09Z [sarocu] DONE. Search-result chips in chat now navigate to their detail view.
- src/components/markdown/mentions.tsx:
  * MENTION_RE: todo date group loosened 'date:([^)]+)' -> 'date:([^)]*)' so empty-date backlog tokens @[t](id:<id>|date:) match and render as chips (REQUIRED pre-step).
  * navigateFromMention: now also handles todo chips (data-todo-id/data-todo-date -> navigateToTodo, empty date routes to backlog) and artifact chips (data-artifact-path -> navigateToArtifact). Returns true when handled. Imports navigateToTodo/navigateToArtifact.
- src/state/store.ts: added pendingArtifactSelection signal + navigateToArtifact(path) (sets signal, activeTab='artifacts').
- src/components/artifacts/ArtifactsPanel.tsx: effect consumes pendingArtifactSelection -> setCwd(parent dir) and setViewing(file) to open the viewer modal; prefers full entry from allArtifacts, falls back to a minimal Artifact; folder paths just setCwd (is_dir guard).
- Render paths already wired: MarkdownRenderer (assistant) + MessageBubble (user) both onClick=navigateFromMention, so all chip kinds are clickable from chat.

Verify: npm run build (root, tsc+vite) clean. Regex unit-checked via node — all 7 token kinds (dated todo, BACKLOG todo w/ empty date, artifact, list, list_item, book, page) match with correct group assignment; empty-date backlog now yields date=''. DayPanel already consumes pendingTodoSelection (dated todos); backlog todo chips switch to the Backlog tab (no per-item highlight yet — there is no pendingBacklogSelection signal; acceptable for v1, candidate follow-up).

NOTE: GUI click-test not possible here — $WAYLAND_DISPLAY=wayland-1 is set but the compositor socket is absent in $XDG_RUNTIME_DIR (/run/user/1000); 'computer' returns 'Could not find wayland compositor' and xdg-open is missing. End-to-end chip-click test (each kind opens correct tab + detail) deferred to an environment with a reachable compositor.
- 2026-05-31T16:18:37Z [sarocu] Review (PARTIAL): chip-click nav verified as logic only (mentions.tsx, store.ts, ArtifactsPanel.tsx) — not exercised in live app (see T-019). Defect to fix: backlog-todo chip navigation only switches to the backlog tab (store.ts:77); it does NOT select/scroll to the specific item. Make backlog-todo nav select + scroll to the target item, matching day-view nav behavior.
