+++
id = "T-019"
title = "Live-verify /search skill menu and chip-click navigation in running Tauri app"
status = "blocked"
priority = 3
created_at = "2026-05-31T16:18:30Z"
updated_at = "2026-05-31T18:18:43Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = "Live click-test impossible in this sandbox: no reachable display server (wayland/X11 socket absent), no headless webdriver installed, and agent live path needs ANTHROPIC_API_KEY + seeded /data. Frontend serves on :1420; static verification stands at PARTIAL. Needs a desktop session to complete."
spec_anchor = ""
+++
Review follow-up (002-search-tools). Verdict capped at PARTIAL because UI click-test was NOT performed — Tauri app was not brought up end-to-end (agent backend + LLM provider + /data). Underlying functions verified only by static read + regex/runtime tests. Drive the running app to confirm: (1) /search built-in skill menu appears in chat, (2) chip rendering, (3) live click navigation actually lands on detail views for todo/artifact/list/book.

## Notes

- 2026-05-31T16:24:25Z [sarocu] PLAN (live-verify /search + chip nav):

Bring-up (3 procs):
1. Agent backend: `npm run dev:agent` (agent/ → tsx watch src/agent.ts). Needs LLM provider env (check agent/src/agent.ts for required key e.g. ANTHROPIC_API_KEY/OPENAI_API_KEY); export before launch. Confirm it binds its port and the `search` tool registers (agent/src/tools/search.ts).
2. Vite UI: `npm run dev`.
3. Desktop shell: `npm run dev:tauri`. If no display in this env, fall back to verifying in the Vite browser build at the dev URL; note that Tauri-only IPC paths (fs/file entities) may not exercise — record as limitation.

Checks (map each to spec task):
- T-016 /search menu: type '/' in ChatInput → menu lists 'Search' (skills.ts BUILTIN_SKILLS, findSkillByTrigger). Type '/search foo' → promptTemplate yields 'Search the workspace for: foo'; systemHint injected.
- T-015 tool: agent calls `search` tool, returns hits across todo/artifact/file/list/book.
- T-017 chips: agent reply renders [[type:id|label]] as MentionChip buttons (mentions.tsx MENTION_RE, classes mention-<type>). Confirm all 5 types render.
- Click nav: click each chip → navigateToEntity (store.ts:55) sets activeView+selectedEntity; lands on today/artifacts/lists/books/artifacts respectively.

Known gap to confirm/flag (followup): backlog-todo chip only switches tab, no scroll/select to item (store.ts:65-67 TODO). If reproduced, file separate follow-up task rather than fixing here.

Verify done = all 5 chip types navigate to correct detail view live. Capture screenshots per type. On success, comment results + mark done. If app cannot be brought up headless, comment the exact blocker (missing display / provider key) and leave open.
- 2026-05-31T16:24:54Z [sarocu] PLAN CORRECTION (verified against actual source — supersedes symbol names above):

Bring-up (no dev:agent/tsx exist):
- Agent: cd agent && npm run build && npm start (node dist/index.js, express). Reads LLM keys from env — agent deps are @anthropic-ai/sdk + openai, so set ANTHROPIC_API_KEY or OPENAI_API_KEY (confirm which in agent/src/agent.ts). Tools resolve data from /data (storage/artifacts/index) — search tool = agent/src/tools/search.ts searchIndex().
- UI: npm run dev (vite). Full desktop: npm run tauri dev. Headless env → drive the vite URL in a browser; Tauri-only IPC (artifact fs reads) may not fire — record as limitation.

Accurate checks:
- T-016 /search menu: ChatInput.tsx handleInput matches /^\/(\S*)$/ at message start → setShowSkills; menu maps SKILLS (skills.ts, currently just {name:'search'}). Enter/Tab/click → selectSkill sets textarea to literal '/search '. There is NO promptTemplate/systemHint — the agent reads the slash convention per its system prompt.
- T-015 tool: send '/search <query>'; agent invokes search tool → ranked hits across todo/backlog/spec/list/list_item/book/page/artifact.
- T-017 chips: agent echoes @[Label](...) tokens; mentionsToHtml (mentions.tsx) → spans .mention / .mention-artifact / .mention-list / .mention-book. 4 visible chip kinds (todo uses bare .mention with data-todo-id).
- Click nav: navigateFromMention (mentions.tsx:25) dispatches by data-* attr → list→navigate (store pendingListSelection+activeTab='lists'); book→pendingBookSelection+'books'; todo→navigateToTodo (date→'day' else 'backlog'); artifact→navigateToArtifact ('artifacts'). No navigateToEntity symbol exists.

Confirmed gap (matches follow-up): backlog todo chip (empty date) → navigateToTodo else-branch just sets activeTab='backlog' (store.ts:77-78); no scroll/select to the item. If repro, file separate follow-up, don't fix here.

Done = all chip kinds (list/list_item, book/page, artifact, dated-todo) navigate to the correct detail view live; screenshot each. If headless blocks bring-up, comment exact blocker (no display / missing provider key) and leave open.
- 2026-05-31T16:28:42Z [sarocu] LIVE-VERIFY ATTEMPT (2026-05-31): cannot perform the interactive click-test in this sandbox. Findings:
- Vite dev server IS up on http://localhost:1420/ serving the live workspace (HTTP 200, /@vite/client present) — frontend builds/serves fine.
- DISPLAY BLOCKER: $WAYLAND_DISPLAY=wayland-1 and $DISPLAY=:1 are exported but NO display server is actually reachable — /run/user/1000 does not exist (no wayland socket), /tmp/.X11-unix absent. 'computer windows/outputs/screenshot' all fail with 'Could not find wayland compositor'; xdg-open can't launch a browser ('computer browse' -> failed to launch xdg-open).
- NO HEADLESS DRIVER: no playwright/puppeteer/geckodriver/chromedriver and no vitest/jest in node_modules; firefox --headless --screenshot hangs (timeout/143) and is one-shot anyway (can't script typing '/' or clicking chips).
- AGENT LIVE PATH (T-015) separately blocked: ANTHROPIC_API_KEY unset (provider uses new Anthropic()), and /data has no seeded todos/lists/books/artifacts (empty index.json), so the agent can't be driven to invoke the search tool even if a display existed.
Static verification stands (prior PARTIAL): symbols confirmed present — skills.ts SKILLS, ChatInput showSkills/selectSkill, mentions.tsx navigateFromMention, store pendingListSelection/pendingBookSelection/navigateToTodo/navigateToArtifact, searchIndex export. Cannot upgrade to FULL without a reachable display + provider key + seeded data. Leaving for an environment that has a desktop session.
- 2026-05-31T18:18:43Z [sarocu] Review follow-up: live-verify still outstanding. Reviewer could not click-test — no wayland compositor (/run/user/1000/wayland* missing, 'computer windows' returns no compositor) and /search end-to-end needs agent backend + LLM key. Confirm on a real display: (1) /search slash menu autocompletes (ChatInput.tsx:213-229, only at message start), (2) live chip-click navigation lands on detail views for todo/artifact/list/book (mentions.tsx:41-49). Pair with the backlog-todo select/scroll fix.
